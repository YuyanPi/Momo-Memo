<Window x:Class="MomoMemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Momo Memo" Width="980" Height="700" MinWidth="820" MinHeight="560"
        Background="#F4F5F8" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo x:Name="TaskbarState"/>
    </Window.TaskbarItemInfo>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#202331" Padding="12">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Momo Memo" Foreground="White" FontSize="22" FontWeight="SemiBold" Margin="8,8,8,18"/>
                <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,0">
                    <Button Content="统计" Click="ShowStats_Click"/>
                    <Button Content="导出" Click="Export_Click"/>
                    <Button Content="设置" Click="Settings_Click"/>
                </StackPanel>
                <ListBox x:Name="ViewsList" Background="Transparent" BorderThickness="0" Foreground="White"
                         DisplayMemberPath="Title" SelectionChanged="ViewsList_SelectionChanged">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="9,7"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="PageTitle" Text="今日" FontSize="28" FontWeight="SemiBold" Foreground="#202331"/>
                    <TextBlock x:Name="SummaryText" Margin="0,5,0,0" Foreground="#6B7280"/>
                </StackPanel>
                <Button Grid.Column="1" Content="＋ 新建任务" Click="AddTask_Click" Background="#6C63FF" Foreground="White" BorderThickness="0" Padding="16,8"/>
            </Grid>

            <Border Grid.Row="1" Background="White" CornerRadius="9" Margin="0,18,0,14" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="QuickTitle" BorderThickness="0" FontSize="14" KeyDown="QuickTitle_KeyDown"
                             ToolTip="只写标题即可快速放入 Inbox"/>
                    <Button Grid.Column="1" Content="快速记入 Inbox" Click="QuickAdd_Click"/>
                </Grid>
            </Border>

            <ListBox Grid.Row="2" x:Name="TasksList" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="10" Padding="14" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox VerticalAlignment="Top" Margin="0,4,12,0" IsChecked="{Binding IsCompleted, Mode=OneWay}" Tag="{Binding Id}" Click="Complete_Click"/>
                                <StackPanel Grid.Column="1">
                                    <DockPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" Foreground="#202331"/>
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding Priority}" Foreground="#EF4444" FontWeight="Bold"/>
                                    </DockPanel>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="#4B5563" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding TimeText}" Foreground="#6B7280" Margin="0,6,0,0"/>
                                    <TextBlock Text="{Binding FlagsText}" Foreground="#6C63FF" Margin="0,3,0,0"/>
                                    <DockPanel Margin="0,9,0,0">
                                        <TextBlock Text="{Binding StatusText}" Foreground="#6B7280" VerticalAlignment="Center"/>
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                            <Button Content="编辑" Tag="{Binding Id}" Click="EditTask_Click"/>
                                            <Button Content="稍后" Tag="{Binding Id}" Click="Snooze_Click" Visibility="{Binding CanSnooze, Converter={StaticResource BoolToVisibility}}"/>
                                            <Button Content="归档/恢复" Tag="{Binding Id}" Click="Archive_Click"/>
                                            <Button Content="删除" Tag="{Binding Id}" Click="Delete_Click" Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisibility}}"/>
                                        </StackPanel>
                                    </DockPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
